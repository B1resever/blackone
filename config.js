/*
 * B1 / BLACK ONE — runtime configuration
 *
 * Google Maps API key.
 * Paste your key between the quotes below. It powers the reservation
 * autocomplete, route preview map, and distance/time calculation.
 *
 * If left empty, the reservation form still works as plain text inputs
 * and the map area shows a clean fallback message.
 *
 * Get a key: https://console.cloud.google.com/google/maps-apis
 * Enable: "Maps JavaScript API", "Places API", "Directions API".
 */
window.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY = "";
